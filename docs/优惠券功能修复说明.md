# 优惠券功能修复说明

## 问题描述
选完优惠券后，预计支付的钱数没有变化，比如199元的课程，选择"满100-30"优惠券后，还是显示199元。

## 问题分析

经过代码审查，发现以下问题：

### 1. 前端价格计算逻辑问题
- 原有的 `payAmount` 计算函数没有正确处理优惠券的使用门槛
- 缺少调试信息，难以排查计算过程
- 优惠券标签显示不够直观

### 2. 后端优惠券过滤逻辑问题
- `CouponServiceImpl.listAvailable()` 方法对不满足门槛的优惠券处理不当
- 返回了不可用的优惠券，导致前端计算错误

## 修复内容

### 1. 前端修复 (`frontend/src/views/course/detail.vue`)

#### ✅ 改进价格计算逻辑
```typescript
const payAmount = computed(() => {
  if (!course.value) return 0
  const price = Number(course.value.price || 0)
  if (!selectedCouponId.value) return price
  
  const c = availableCoupons.value.find((it) => String(it.userCouponId) === String(selectedCouponId.value))
  if (!c) return price
  
  // 检查使用门槛
  const thresholdAmount = Number(c.thresholdAmount || 0)
  if (thresholdAmount > 0 && price < thresholdAmount) {
    console.warn(`优惠券使用门槛不满足：需要¥${thresholdAmount}，当前¥${price}`)
    return price
  }
  
  let discount = 0
  if (c.type === 1) {
    // 满减券
    discount = Number(c.discountAmount || 0)
  } else if (c.type === 2) {
    // 折扣券
    const rate = Number(c.discountRate || 1)
    discount = price - Number((price * rate).toFixed(2))
  }
  
  // 确保折扣不超过商品价格
  if (discount < 0) discount = 0
  if (discount > price) discount = price
  
  const finalPrice = Number((price - discount).toFixed(2))
  console.log('价格计算:', { 原价: price, 优惠券: c.name, 门槛: thresholdAmount, 折扣: discount, 最终价格: finalPrice })
  
  return finalPrice
})
```

#### ✅ 优化优惠券标签显示
```typescript
const couponLabel = (c: any) => {
  if (!c) return ''
  
  const price = Number(course.value?.price || 0)
  let savingText = ''
  
  if (c.type === 1) {
    // 满减券：显示减免金额和门槛
    const discountAmount = Number(c.discountAmount || 0)
    const threshold = Number(c.thresholdAmount || 0)
    savingText = `减¥${discountAmount}`
    if (threshold > 0) {
      savingText += ` (满¥${threshold})`
    }
    return `${c.name || '满减券'} - ${savingText}`
  }
  
  if (c.type === 2) {
    // 折扣券：显示折扣和实际节省金额
    const rate = Number(c.discountRate || 1)
    const threshold = Number(c.thresholdAmount || 0)
    savingText = `${Math.round(rate * 100)}折`
    if (threshold > 0) {
      savingText += ` (满¥${threshold})`
    }
    // 计算实际节省金额
    if (price > 0) {
      const actualSaving = price - (price * rate)
      savingText += ` 省¥${actualSaving.toFixed(2)}`
    }
    return `${c.name || '折扣券'} - ${savingText}`
  }
  
  return c.name || '优惠券'
}
```

#### ✅ 添加调试功能
- 添加优惠券选择变化监听
- 添加价格计算过程日志
- 优化错误提示

### 2. 后端修复 (`backend/order-service/.../CouponServiceImpl.java`)

#### ✅ 优化优惠券过滤逻辑
```java
// 门槛过滤 - 直接过滤掉不满足条件的优惠券
if (c != null && c.getThresholdAmount() != null && orderAmount.compareTo(c.getThresholdAmount()) < 0) {
    return null; // 不满足门槛条件的直接过滤掉
}
return vo;
}).filter(vo -> vo != null && (vo.getStatus() == null || vo.getStatus() == 1)) // 只返回可用的优惠券
```

**改进说明**：
- 原来是标记为不可用但仍然返回，现在直接过滤掉
- 确保前端只收到真正可用的优惠券

## 测试数据

数据库中已包含测试优惠券：

```sql
-- 满减券：满100减30
INSERT INTO t_coupon (id, name, type, discount_amount, threshold_amount, ...)
VALUES (6001, '满100减30', 1, 30.00, 100.00, ...);

-- 折扣券：95折通用
INSERT INTO t_coupon (id, name, type, discount_rate, threshold_amount, ...)
VALUES (6002, '95折通用', 2, 0.95, 0.00, ...);

-- 用户优惠券
INSERT INTO t_user_coupon (id, coupon_id, user_id, status, ...)
VALUES 
    (6101, 6001, 1001, 0, ...),  -- 用户1001拥有满100减30
    (6102, 6002, 1001, 0, ...);  -- 用户1001拥有95折通用
```

## 测试步骤

### 1. 测试满减券
1. 登录用户ID为1001的账号
2. 浏览价格≥100元的课程（如199元课程）
3. 选择"满100减30"优惠券
4. 验证预计支付金额变为169元（199-30=169）

### 2. 测试折扣券
1. 选择"95折通用"优惠券
2. 验证预计支付金额变为189.05元（199×0.95=189.05）

### 3. 测试门槛限制
1. 浏览价格<100元的课程
2. 验证"满100减30"优惠券不出现在可选列表中
3. 验证"95折通用"优惠券正常显示（无门槛限制）

## 预期效果

### 用户界面改进
- **优惠券选择更直观**：显示实际节省金额
- **价格变化实时显示**：选择优惠券后立即看到最终价格
- **门槛提示清晰**：不满足条件的优惠券不会显示

### 计算逻辑修复
- **满减券**：199元课程 + 满100减30 = 169元
- **折扣券**：199元课程 + 95折 = 189.05元
- **门槛检查**：价格不满足门槛的优惠券不显示

## 调试信息

在浏览器开发者工具的Console中可以看到：
- 加载到的优惠券列表
- 选择优惠券时的详细信息
- 价格计算过程（原价、优惠券、门槛、折扣、最终价格）

## 注意事项

1. **浏览器缓存**：修改后建议清除浏览器缓存或硬刷新
2. **用户登录**：必须登录才能看到优惠券选项
3. **优惠券状态**：确保测试用的优惠券状态为"未使用"且在有效期内
4. **调试日志**：生产环境建议移除console.log调试信息

## 相关文件

### 修改的文件
- `frontend/src/views/course/detail.vue` - 课程详情页优惠券逻辑
- `backend/order-service/.../CouponServiceImpl.java` - 优惠券服务实现

### 相关API
- `GET /api/order/coupon/available` - 获取可用优惠券
- `POST /api/order/create` - 创建订单（使用优惠券）

## 后续优化建议

1. **优惠券叠加**：支持多张优惠券同时使用
2. **优惠券推荐**：智能推荐最优优惠券组合
3. **限时优惠**：添加限时优惠券功能
4. **优惠券分享**：支持优惠券分享给好友
