# 第一阶段详细实施方案 - 核心业务闭环完善

## 🎯 **阶段目标**
完善学习体验闭环，实现课程互动功能，提升用户粘性和平台价值。

**预计时间：** 2-3周  
**优先级：** 🔥🔥🔥🔥🔥 (最高)

---

## 📋 **具体任务清单**

### 任务1：学习服务 (learning-service) 完善

#### 🎯 **目标**
建立完整的学习数据追踪和分析体系，为用户提供个性化学习报告。

#### 📊 **核心功能**

1. **学习进度追踪**
   - 视频播放进度记录
   - 课程完成状态管理
   - 学习时长统计

2. **学习数据分析**
   - 每日/周/月学习统计
   - 学习习惯分析
   - 完成率计算

3. **学习报告生成**
   - 个人学习报告
   - 学习成就展示
   - 学习建议推荐

#### 🛠 **技术实现**

**项目结构：**
```
backend/learning-service/
├── src/main/java/com/taotao/education/learning/
│   ├── LearningServiceApplication.java
│   ├── controller/
│   │   ├── StudyRecordController.java
│   │   ├── LearningStatsController.java
│   │   └── LearningReportController.java
│   ├── service/
│   │   ├── StudyRecordService.java
│   │   ├── LearningStatsService.java
│   │   └── LearningReportService.java
│   ├── entity/
│   │   ├── StudyRecord.java
│   │   ├── LearningStats.java
│   │   └── LearningReport.java
│   ├── mapper/
│   │   ├── StudyRecordMapper.java
│   │   └── LearningStatsMapper.java
│   ├── dto/
│   │   ├── StudyProgressDTO.java
│   │   └── LearningReportDTO.java
│   └── vo/
│       ├── StudyRecordVO.java
│       └── LearningStatsVO.java
└── src/main/resources/
    └── application.yml
```

**数据库设计：**
```sql
-- 学习记录表 (详细记录每次学习行为)
CREATE TABLE t_study_record (
    id BIGINT PRIMARY KEY COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    course_id BIGINT NOT NULL COMMENT '课程ID',
    lesson_id BIGINT NOT NULL COMMENT '课时ID',
    study_duration INT DEFAULT 0 COMMENT '本次学习时长(秒)',
    video_duration INT DEFAULT 0 COMMENT '视频总时长(秒)',
    last_position INT DEFAULT 0 COMMENT '最后播放位置(秒)',
    progress_percent DECIMAL(5,2) DEFAULT 0.00 COMMENT '播放进度百分比',
    is_completed TINYINT DEFAULT 0 COMMENT '是否完成 0-未完成 1-已完成',
    study_date DATE NOT NULL COMMENT '学习日期',
    device_type VARCHAR(20) COMMENT '设备类型 web/mobile/app',
    ip_address VARCHAR(45) COMMENT 'IP地址',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_user_course (user_id, course_id),
    INDEX idx_user_date (user_id, study_date),
    INDEX idx_lesson (lesson_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习记录表';

-- 课程学习统计表 (聚合统计数据)
CREATE TABLE t_learning_stats (
    id BIGINT PRIMARY KEY COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    course_id BIGINT NOT NULL COMMENT '课程ID',
    total_lessons INT DEFAULT 0 COMMENT '课程总课时数',
    completed_lessons INT DEFAULT 0 COMMENT '已完成课时数',
    total_duration INT DEFAULT 0 COMMENT '课程总时长(秒)',
    study_duration INT DEFAULT 0 COMMENT '已学习时长(秒)',
    completion_rate DECIMAL(5,2) DEFAULT 0.00 COMMENT '完成率',
    first_study_time DATETIME COMMENT '首次学习时间',
    last_study_time DATETIME COMMENT '最后学习时间',
    study_days INT DEFAULT 0 COMMENT '学习天数',
    avg_daily_duration INT DEFAULT 0 COMMENT '日均学习时长(秒)',
    is_finished TINYINT DEFAULT 0 COMMENT '是否完成课程 0-未完成 1-已完成',
    certificate_id BIGINT COMMENT '证书ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY uk_user_course (user_id, course_id),
    INDEX idx_user (user_id),
    INDEX idx_completion (completion_rate)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程学习统计表';

-- 用户学习总统计表
CREATE TABLE t_user_learning_stats (
    id BIGINT PRIMARY KEY COMMENT '主键ID',
    user_id BIGINT NOT NULL UNIQUE COMMENT '用户ID',
    total_courses INT DEFAULT 0 COMMENT '总学习课程数',
    completed_courses INT DEFAULT 0 COMMENT '已完成课程数',
    total_study_duration INT DEFAULT 0 COMMENT '总学习时长(秒)',
    total_study_days INT DEFAULT 0 COMMENT '总学习天数',
    avg_daily_duration INT DEFAULT 0 COMMENT '日均学习时长(秒)',
    longest_streak INT DEFAULT 0 COMMENT '最长连续学习天数',
    current_streak INT DEFAULT 0 COMMENT '当前连续学习天数',
    last_study_date DATE COMMENT '最后学习日期',
    level_score INT DEFAULT 0 COMMENT '学习等级积分',
    certificates_count INT DEFAULT 0 COMMENT '获得证书数量',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_level (level_score),
    INDEX idx_last_study (last_study_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户学习总统计表';
```

**核心API设计：**
```java
@RestController
@RequestMapping("/api/learning")
public class StudyRecordController {
    
    // 记录学习进度
    @PostMapping("/progress")
    public Result<Void> recordProgress(@RequestBody StudyProgressDTO dto);
    
    // 获取课程学习统计
    @GetMapping("/stats/{courseId}")
    public Result<LearningStatsVO> getCourseStats(@PathVariable Long courseId);
    
    // 获取用户学习报告
    @GetMapping("/report")
    public Result<LearningReportVO> getLearningReport(
        @RequestParam(defaultValue = "week") String period);
    
    // 获取学习记录列表
    @GetMapping("/records")
    public Result<Page<StudyRecordVO>> getStudyRecords(
        @RequestParam(required = false) Long courseId,
        @RequestParam(defaultValue = "1") Integer pageNum,
        @RequestParam(defaultValue = "10") Integer pageSize);
}
```

---

### 任务2：评价问答服务 (review-qa-service)

#### 🎯 **目标**
建立课程互动生态，提升用户参与度和课程质量。

#### 📊 **核心功能**

1. **课程评价系统**
   - 五星评分机制
   - 文字评价内容
   - 匿名评价选项
   - 评价审核机制

2. **课程问答系统**
   - 学员提问功能
   - 讲师/学员回答
   - 答案采纳机制
   - 问答搜索功能

3. **互动数据统计**
   - 评价统计分析
   - 问答活跃度统计
   - 讲师回复率统计

#### 🛠 **技术实现**

**数据库设计：**
```sql
-- 课程评价表
CREATE TABLE t_course_review (
    id BIGINT PRIMARY KEY COMMENT '主键ID',
    course_id BIGINT NOT NULL COMMENT '课程ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    order_id BIGINT COMMENT '订单ID',
    score TINYINT NOT NULL COMMENT '评分 1-5星',
    content TEXT COMMENT '评价内容',
    is_anonymous TINYINT DEFAULT 0 COMMENT '是否匿名 0-否 1-是',
    status TINYINT DEFAULT 1 COMMENT '状态 0-待审核 1-已通过 2-已拒绝',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    reply_count INT DEFAULT 0 COMMENT '回复数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY uk_user_course (user_id, course_id),
    INDEX idx_course_score (course_id, score),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程评价表';

-- 课程问答表
CREATE TABLE t_course_question (
    id BIGINT PRIMARY KEY COMMENT '主键ID',
    course_id BIGINT NOT NULL COMMENT '课程ID',
    lesson_id BIGINT COMMENT '课时ID',
    user_id BIGINT NOT NULL COMMENT '提问用户ID',
    title VARCHAR(200) NOT NULL COMMENT '问题标题',
    content TEXT NOT NULL COMMENT '问题内容',
    status TINYINT DEFAULT 1 COMMENT '状态 0-待审核 1-已发布 2-已关闭',
    is_resolved TINYINT DEFAULT 0 COMMENT '是否已解决 0-未解决 1-已解决',
    view_count INT DEFAULT 0 COMMENT '浏览次数',
    answer_count INT DEFAULT 0 COMMENT '回答数量',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_course (course_id),
    INDEX idx_user (user_id),
    INDEX idx_status_resolved (status, is_resolved)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程问答表';

-- 问答回答表
CREATE TABLE t_question_answer (
    id BIGINT PRIMARY KEY COMMENT '主键ID',
    question_id BIGINT NOT NULL COMMENT '问题ID',
    user_id BIGINT NOT NULL COMMENT '回答用户ID',
    content TEXT NOT NULL COMMENT '回答内容',
    is_accepted TINYINT DEFAULT 0 COMMENT '是否被采纳 0-否 1-是',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    status TINYINT DEFAULT 1 COMMENT '状态 0-待审核 1-已发布 2-已删除',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_question (question_id),
    INDEX idx_user (user_id),
    INDEX idx_accepted (is_accepted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='问答回答表';
```

**核心API设计：**
```java
@RestController
@RequestMapping("/api/review")
public class ReviewController {
    
    // 提交课程评价
    @PostMapping
    public Result<Void> addReview(@RequestBody CourseReviewDTO dto);
    
    // 获取课程评价列表
    @GetMapping("/course/{courseId}")
    public Result<Page<CourseReviewVO>> getCourseReviews(@PathVariable Long courseId);
    
    // 评价点赞
    @PostMapping("/{reviewId}/like")
    public Result<Void> likeReview(@PathVariable Long reviewId);
}

@RestController
@RequestMapping("/api/qa")
public class QaController {
    
    // 提交问题
    @PostMapping("/question")
    public Result<Long> addQuestion(@RequestBody QuestionDTO dto);
    
    // 回答问题
    @PostMapping("/answer")
    public Result<Long> addAnswer(@RequestBody AnswerDTO dto);
    
    // 采纳答案
    @PostMapping("/answer/{answerId}/accept")
    public Result<Void> acceptAnswer(@PathVariable Long answerId);
    
    // 获取课程问答列表
    @GetMapping("/course/{courseId}")
    public Result<Page<QuestionVO>> getCourseQuestions(@PathVariable Long courseId);
}
```

---

### 任务3：搜索服务 (search-service)

#### 🎯 **目标**
提升内容发现能力，帮助用户快速找到需要的课程。

#### 📊 **核心功能**

1. **全文搜索**
   - 课程标题搜索
   - 课程描述搜索
   - 讲师名称搜索
   - 智能分词匹配

2. **高级筛选**
   - 分类筛选
   - 价格区间筛选
   - 评分筛选
   - 难度等级筛选

3. **排序功能**
   - 相关度排序
   - 最新发布排序
   - 最受欢迎排序
   - 评分排序
   - 价格排序

4. **搜索推荐**
   - 热门搜索词
   - 搜索建议
   - 相关搜索

#### 🛠 **技术实现**

**技术栈选择：**
- Elasticsearch 7.x (搜索引擎)
- Spring Data Elasticsearch (数据访问)
- IK Analyzer (中文分词)

**项目结构：**
```
backend/search-service/
├── src/main/java/com/taotao/education/search/
│   ├── SearchServiceApplication.java
│   ├── controller/
│   │   └── SearchController.java
│   ├── service/
│   │   ├── CourseSearchService.java
│   │   └── SearchSuggestionService.java
│   ├── document/
│   │   └── CourseDocument.java
│   ├── repository/
│   │   └── CourseSearchRepository.java
│   └── dto/
│       ├── SearchRequestDTO.java
│       └── SearchResultDTO.java
└── src/main/resources/
    ├── application.yml
    └── elasticsearch/
        └── course-mapping.json
```

**Elasticsearch文档结构：**
```json
{
  "mappings": {
    "properties": {
      "id": {"type": "long"},
      "title": {
        "type": "text",
        "analyzer": "ik_max_word",
        "search_analyzer": "ik_smart"
      },
      "description": {
        "type": "text",
        "analyzer": "ik_max_word"
      },
      "teacherName": {
        "type": "text",
        "analyzer": "ik_smart"
      },
      "categoryId": {"type": "long"},
      "categoryName": {
        "type": "keyword"
      },
      "tags": {
        "type": "keyword"
      },
      "price": {"type": "double"},
      "originalPrice": {"type": "double"},
      "score": {"type": "float"},
      "studyCount": {"type": "integer"},
      "lessonCount": {"type": "integer"},
      "duration": {"type": "integer"},
      "level": {"type": "keyword"},
      "isFree": {"type": "boolean"},
      "status": {"type": "integer"},
      "createTime": {"type": "date"},
      "updateTime": {"type": "date"}
    }
  }
}
```

**核心API设计：**
```java
@RestController
@RequestMapping("/api/search")
public class SearchController {
    
    // 课程搜索
    @GetMapping("/courses")
    public Result<SearchResultDTO> searchCourses(
        @RequestParam(required = false) String keyword,
        @RequestParam(required = false) Long categoryId,
        @RequestParam(required = false) String priceRange,
        @RequestParam(required = false) Float minScore,
        @RequestParam(required = false) String level,
        @RequestParam(defaultValue = "relevance") String sortBy,
        @RequestParam(defaultValue = "1") Integer pageNum,
        @RequestParam(defaultValue = "20") Integer pageSize);
    
    // 搜索建议
    @GetMapping("/suggest")
    public Result<List<String>> getSearchSuggestions(
        @RequestParam String keyword);
    
    // 热门搜索
    @GetMapping("/hot-keywords")
    public Result<List<String>> getHotKeywords();
    
    // 同步课程数据到ES
    @PostMapping("/sync/courses")
    public Result<Void> syncCourses();
}
```

---

## 🚀 **前端配套开发**

### 1. **学习中心页面**
```
frontend/src/views/study/
├── center.vue          # 学习中心首页
├── progress.vue        # 学习进度页面
├── report.vue          # 学习报告页面
└── records.vue         # 学习记录页面
```

### 2. **课程详情页增强**
- 评价Tab页面
- 问答Tab页面
- 学习进度显示
- 相关推荐课程

### 3. **搜索页面**
```
frontend/src/views/search/
├── index.vue           # 搜索结果页
├── components/
│   ├── SearchBox.vue   # 搜索框组件
│   ├── FilterPanel.vue # 筛选面板
│   └── CourseCard.vue  # 课程卡片
```

---

## 📊 **数据迁移方案**

### 1. **现有数据处理**
- 将现有用户课程关联数据迁移到学习统计表
- 为已有课程初始化评分数据
- 创建Elasticsearch索引并同步课程数据

### 2. **数据同步策略**
- 课程数据变更时自动同步到ES
- 学习记录实时更新统计数据
- 评价数据实时更新课程评分

---

## 🧪 **测试方案**

### 1. **单元测试**
- 每个Service方法的单元测试
- 覆盖率要求 > 80%

### 2. **集成测试**
- API接口测试
- 数据库操作测试
- Elasticsearch集成测试

### 3. **性能测试**
- 搜索接口性能测试
- 学习记录批量写入测试
- 并发访问测试

---

## 📈 **监控指标**

### 1. **业务指标**
- 用户学习时长增长率
- 课程完成率提升
- 评价参与率
- 问答活跃度

### 2. **技术指标**
- 搜索响应时间 < 200ms
- 学习记录写入延迟 < 100ms
- 系统可用性 > 99.9%

---

## 🎯 **成功标准**

### 功能完整性
- ✅ 学习进度准确追踪
- ✅ 评价问答正常运行
- ✅ 搜索功能满足需求

### 性能要求
- ✅ 搜索响应时间 < 200ms
- ✅ 支持1000+并发用户
- ✅ 数据一致性保证

### 用户体验
- ✅ 界面友好易用
- ✅ 功能流程顺畅
- ✅ 数据展示直观

这个详细方案确保第一阶段能够顺利实施，为后续阶段奠定坚实基础！
